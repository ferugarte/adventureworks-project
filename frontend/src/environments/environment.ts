export const environment = {
  production: false,  // Indicates that this environment is for development purposes
  baseUrl: 'http://localhost:3000'  // Base URL for the backend API in a local environment
};
